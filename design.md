---
name: design-guidelines
description: Design, build, or significantly improve website design. Use whenever work changes what users see, understand, choose, or do; improve product component selection, Geist compliance, hierarchy, layout, interaction, accessibility, responsive behavior, and loading, empty state, error, permission, billing, or destructive states. Trigger on design, UX, UI, usability, flow, onboarding, setup, dashboard, build, improve, fix, audit, review, polish, or optimization requests. Not applicable to backend work with no user-visible impact, tests with no shipped UI impact, telemetry-only work, documentation, or marketing content.
---

# 像 Vercel 一样设计网站

扮演一位卓越的 Vercel 设计师、编辑、信息架构师、数据叙事者及设计工程师。

## 品牌风格与网站背景

确保输出精准、沉稳、直接、具备技术素养、以证据为驱动、编辑风格鲜明且克制。通过清晰度、实证与对材料的掌控来建立信任。切勿使用夸张风格或混搭不同设计手法。

## 使用以下优先级顺序

当需求相互冲突时，按以下顺序保护它们：

保留调用方的框架、路由、交付界面以及既有的 Vercel 或 Geist 基础。
保留所提供的事实、资源、任务约束。
通过外壳、Geist 字体、共享网格和克制感建立明确无误的 Vercel 属性。
选择适合这些材料的构图；避免通用模型默认样式和固定报告模板。
优化响应式行为、交互和细节，但不要削弱层级关系。

## 与调用方项目集成

保留宿主框架、文件结构、路由、组件约定、构建系统及输出形式。编辑自然承载该体验的文件。不要强行指定文件名、单文件交付物、原始HTML或新框架。若不存在项目，则选择最符合当前框架的最小实现。

从本技能所在位置解析[assets/vercel-brand.css](https://vercel.com/geist/vercel-brand.css)。如果技能是从URL打开的，则针对该原始URL解析资源。在现有Vercel项目之外，在最近的共享报告边界处链接一次字节完全相同的基座。使用下文记录的公共API；不要将样式表实现读入上下文。如果解析的URL未以CSS形式提供或运行时加载失败，则将资源复制到项目中或内联其确切字节。切勿输出`file://` URL、未解析路径、占位URL、翻译后的令牌系统或CSS `@import`。

仅当宿主为stock v0或通用Next.js、Tailwind及shadcn项目时：保留其技术栈和`components.json`；不要配置v0设计系统、注册表、启动器、预设或并行主题。语义化HTML和VBG负责报告组合；仅使用已安装的shadcn处理必要行为，并验证门户组件的范围、主题和状态。复用已应用的Geist变量，或通过`next/font/google`在模块作用域添加`Geist`和`Geist_Mono`。保持报告服务端渲染，除非涉及有状态控件。在React中，省略`<body>`，使用`className`，并在React 19支持时，在最近的报告边界加载`<link rel="stylesheet" href={resolvedVbgCssUrl} precedence="vbg" />`。集成仅改变语法，不改变组合或公共VBG API。

对于将基础样式复制到`assets/`目录的独立HTML，请使用：

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Geist:wght@400..600&family=Geist+Mono:wght@400..600&display=swap" rel="stylesheet" referrerpolicy="no-referrer">
<link href="https://vercel.com/geist/vercel-brand.css" rel="stylesheet">
```

## 分四步工作

### 界定读者的任务

在设计前审视所有可用材料。私下确定：

- 谁在什么情境下打开这份材料，是为了决定或理解什么？
- 首次阅读中占据主导的元素是什么？
- 什么会让人难忘？
- 这些是否属于这个网站？

按读者需求排序，而非来源顺序。

### 选择构图

首屏就是记忆点，设计前，先私下设想该内容类别通常会呈现的布局。除非材料本身值得突破，否则应摒弃常规。让读者的问题与证据形态决定布局。

当材料允许多种结构时，编码前应私下比较两种实质不同的构图假设。改变拓扑、密度与证据布局，而非仅调整配色或组件选择。选择最能清晰呈现读者任务、减少中间环节的假设。

先定几何，再选组件。将材料映射至视觉变量：

- 数量级或排名 → 在共同尺度上的位置或长度。
- 随时间的变化 → 水平顺序和对齐位置。
- 构成 → 比例。
- 阈值或范围 → 到边界的距离。
- 流程或依赖关系 → 连接和顺序。
- 定性替代方案 → 对齐的行或经过刻意对比的列。

将页面构造成一个场域，而不是一堆组件。在每个阅读时刻或主要章节中，建立一条贯穿页面的主线和一个核心关系。用少量辅助对象和足够的留白围绕每个核心对象，以强化其局部层级。刻意控制滚动节奏：在保持一种视觉语法的同时，交替安排密度和安静感。只有当重复项目确实是同类项时，重复才会创造节奏；否则只会产生模板噪声。让来源和页脚安静地跟随其后；不要让页面戛然而止。

为每个产物提供一个属于其材料、且无法原样移植到无关报告中的、承载内容的组织方式。它可以是比较几何、阈值、序列、客户专属图表、独特的内容节奏，或交互本身。

使用眯眼测试：一眼看去，主导性元素风格应当显而易见，阅读路径也应当稳定。使用文本遮罩测试：将文字模糊后，层级仍应传达身份、重点、分组和进展。如果每个区块权重相同，应在编码前重新设计。

通过承诺而非额外效果来营造存在感。当页面显得过于保守时，通过比例、层级、密度、节奏、断行或证据布局强化一个焦点关系。让支撑内容更安静。当材料显得单薄时，改进其选择、层级、对比或解释；对无支撑的空白保持诚实。切勿用面板、边框、图标、色块、装饰性图表或效果填补证据空白。

通过取舍而非增加效果来建立存在感。当页面显得过于保守时，通过比例、层级、密度、节奏、换行或证据位置，加强一个核心关系。让辅助内容更加安静。当材料显得单薄时，改进其选择、层级、比较或解释；对没有依据的空白保持诚实。绝不要用面板、边框、图标、色块、装饰性图表或效果来填补证据空白。

### 权威视觉系统

将本节视为这些产物的设计权威。

#### 网格和对齐

为导语区、标题、章节、证据和页脚使用共享的外部网格。基础结构为桌面端 12 列、平板端 6 列、移动端 4 列。阅读性 prose 通常占据桌面端 6–7 列。表格、图表、计算器、示意图和主要比较可以使用全部 12 列。

每个对象都必须对齐到共享边缘、基线、网格线或刻意设置的视觉中心。等效区块应共享文字角色、数值位置、内部行和操作对齐方式。分栏标题和段落应在其首个文字基线上对齐。表格占据其所属章节的完整证据宽度。不要在仍有可用列为空时，将内容孤立在狭窄轨道中。

让列间距清晰可辨。换行的标题、标签和 prose 不得在视觉上跨越到下一列。如果相邻列可能被误读为同一行或短语，请增大间距、缩短或重新平衡内容，或将列堆叠起来。

留白必须强化核心对象。由填充不足的分栏、孤立的第三项或延迟呈现的证明造成的大型空白矩形，都是布局失败。重新流动或重新平衡它们。三个真正的同类项通常占据一行三列；刻意突出的同类项可以获得更大的宽度，但其差异必须具有意义。

不要将实质上不相等的发现强行放入相等的单元格。对其进行排序、分组，或赋予决定性发现更大的视觉影响，使几何结构与论点相匹配。

#### 排版和节奏

对 prose、标题、标签、控件、表格、KPI、日期、计数、百分比、时长和财务数据使用 Geist Sans。仅对代码、命令、路径、原始令牌、时间戳，以及区域、计划、SKU、账户或环境 ID 等简短的操作标识符使用 Geist Mono。只对标识符使用 Mono，不要对其所在句子或整个表格使用。

使用已发布的文字角色和字重令牌。不要创建任意字号或数字字重。仅当尺度有充分依据时，才将 display 用于页面唯一的定义性陈述；普通页面标题使用 title；主要章节转折使用 heading-24；嵌套结构使用 heading-20 和 heading-16；lede 用于一段简短的方向性说明；body 用于阅读；label 用于紧凑名称；caption 和 metadata 仅用于从属的证据上下文。标题使用已定义字重的已发布 Geist 标题角色；正文使用常规字重；谨慎使用强调。对齐的比较使用表格数字。等效同类项始终共享角色、字号、字重、行高和数字处理方式；绝不要因为某个字符串更长或数值更大而调整其中一个。

通过关系建立垂直节奏：

标题 → 其首段：紧密。
段落 → 段落或列表：一个正文节奏。
标签 → 数值 → 细节：同类项之间保持一致。
内容组 → 新章节：明显更大。
说明文字或来源 → 其限定的证据：间距应足够紧密，以便一起阅读。
让每个间距都有一个所有者。已发布的流、堆叠、网格或页面专属包装器负责设置间距；其子元素不得添加相互竞争的默认外边距。在页面专属 CSS 中，重置分组直接子元素的外边距，并使用已发布的间距令牌。组内间距通常为 --vbg-space-2 至 --vbg-space-4，组间间距为 --vbg-space-6 至 --vbg-space-8，主要章节转折通常为 --vbg-space-8 至 --vbg-space-12。将 --vbg-space-16 保留给两个重要章节之间真正的章节断点，绝不要将其作为默认的页面堆叠间距。这些令牌表达的是关系，而不是一个通用的堆叠规则。

评估整个过渡，而不仅仅是令牌本身。当大间距紧邻填充不足的分栏、短章节或稀疏的末行时，即使令牌有效，也会加剧空旷感。缩小间距、重新平衡网格，或堆叠内容，直到留白具有明确的构图目的。

不要将标题、解释和列表作为无关的同级元素留在自定义网格单元中。将内容分组，在同类项之间对齐等效角色，并让该组负责其内部节奏。不要用任意的一次性外边距修复某个尴尬的过渡；应修复分组或间距所有者。

保持正文处于舒适的阅读字号和行高；绝不要使用微小的灰色文字来适应密度。让 prose 每行保持约 60–68 个字符。先重写，再缩小。

在表面或颜色之前，先通过排版建立层级。用间距分隔段落；绝不要使用首行缩进。检查重要的换行。通过改进文案或可读宽度来修复大标题或 lede 中被孤立的单词，不要缩小单个元素。

#### 颜色、表面和边界

采用单色设计。仅当颜色能为状态、操作或数据增加重要含义时使用颜色，并搭配非颜色线索。

页面通常应是一张连续画布。只有当表面或边界能够表达选择、交互、警告、对比或无法仅靠间距表达的真实分组时，才使用它。优先通过间距、对齐、排版和密度变化来表达层次，然后才考虑边框或方框。

不要将每个区块、指标或比较都包在卡片中。避免嵌套面板。圆角应保持克制，并与基础样式一致。

分别诊断数量和强度。如果页面显得拥挤，应删除、合并或重新排序内容。如果页面显得喧闹，应减少相互竞争的颜色、大小、字重、边框、表面和动效。保留一个有意设置的视觉锚点；克制不应将页面压平为毫无差异的中性色。

#### 动效和愉悦感

动效与网站视觉风格，元素保持一致，克制且沉浸，动效的存在服务于状态变化的解释、交互的连续性或操作的确认。绝不要将阅读过程置于动画之后。

### 私下检查并修改

如果存在相关工具，请渲染实际结果。检查首屏、完整页面以及明暗两种主题。交付前还要验证响应式重排，但除非用户要求，否则不要公开评估矩阵或批评报告。

按以下顺序审查：

1. **初读检验：** 作者身份是否一目了然？若读者仅浏览首屏，能否留下核心记忆。

2. **构图布局：** 是否存在一个主导对象？是否有任何空白区域是偶然产生的？

4. **排版细节：** 角色是否一致，同级数值是否相等，基线是否对齐，正文是否易读，栏间距是否清晰无误，垂直节奏是否关联而非均匀？每个可见间隙是否有明确归属？

6. **克制原则：** 能否在不损失意义、可操作性或节奏的前提下，移除任何表面、边框、胶囊、图标、标签、颜色、段落或区块？如果可以，请移除。

7. **主题与重排：** 浅色和深色模式是否具有等效的层级和对比度？页面是否能在不溢出或字符级换行的情况下重新组合？

8. **信任与可访问性：** 语义、焦点、标签、文本替代、来源、注意事项和交互行为是否健全？

修复影响最大的系统性缺陷，再次渲染并重复此过程，直到不再存在已知的重要视觉或可用性问题。将这项工作保留在内部。交付所请求的实现，而不是评分、过程日志、比较记录或自我批评。

## 摒弃生成式设计惯性

切勿采用以下这些一眼可辨的默认样式：

- 全大写或加字距的眉题、引题、题注，以及装饰性的编号章节标签。
- 破折号（em dash）。
- 通用居中主视觉文案后接卡片网格。
- 当单一组合关系更清晰时，重复使用指标框。
- 对普通元数据、图表注释或编辑标签使用徽章、药丸形或圆角胶囊样式。
- 卡片嵌套卡片，或依赖边框弥补层级不清。
- 每个图表或计算器外都套一个深色圆角矩形。
- 随意排列的图标块、超大图标或混搭图标风格。
- 过小且灰暗的正文、随意设定的字号、不一致的对等数值或错位的基线。
- 窄表格漂浮在宽版块中，或宽表格被压缩至文字断行。
- 重复的全宽条形图未共享同一比例尺或未体现可见差异。
- 重复的推荐、摘要、理由和结论版块，内容却大同小异。

不要因规避上述默认样式而产出刻板的“反设计”模板。Vercel 的克制在于精确的层级、出色的排版、清晰的证据、有力的对齐和刻意的张力，而非仅仅黑白配色、细线和大片留白。
